import type { Metadata } from "next";
import { Geist, Geist_Mono, Inter } from "next/font/google";
import "./globals.css";
import { cn } from "@/lib/utils";

const inter = Inter({subsets:['latin'],variable:'--font-sans'});

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Factor F | Farándula, deporte y conversación",
  description:
    "Factor F es la revista viva de Marie y Máximo Ferro: farándula, fútbol, actualidad y conversaciones con acento venezolano.",
  openGraph: {
    title: "Factor F | Farándula, deporte y conversación",
    description:
      "Farándula, fútbol, actualidad y momentos que todos terminan comentando con Marie y Máximo Ferro.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="es"
      className={cn("h-full", "antialiased", geistSans.variable, geistMono.variable, "font-sans", inter.variable)}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
